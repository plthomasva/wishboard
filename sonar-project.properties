sonar.projectKey=plthomasva_wishboard
sonar.organization=plthomasva

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=wishboard
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=src,scripts

# Coverage reports
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

# Exclude tests and mock data from being analyzed as production code
sonar.exclusions=**/*.test.js,**/*.test.ts,**/*.test.tsx,**/demoSeeder.js
